info:
  name: Update Product attribute
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/attribute
  body:
    type: json
    data: |-
      {
        "id":1,
        "name":"Weight",
        "value":"2Kg",
        "productId":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
