info:
  name: Add Attribute
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/attribute
  body:
    type: json
    data: |-
      {
        "name":"Size",
        "value":"6.3Cal",
        "productId":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
